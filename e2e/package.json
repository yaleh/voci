{
  "name": "voci-e2e",
  "version": "1.0.0",
  "description": "Playwright e2e and UI unit tests for voci",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.3.0"
  }
}
